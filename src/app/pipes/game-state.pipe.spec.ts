import { GameStatePipe } from './game-state.pipe';

describe('GameStatePipe', () => {
  it('create an instance', () => {
    const pipe = new GameStatePipe();
    expect(pipe).toBeTruthy();
  });
});
